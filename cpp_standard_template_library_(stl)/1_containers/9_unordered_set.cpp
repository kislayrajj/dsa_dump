#include <bits/stdc++.h>
using namespace std;

void explain_unordered_set();

int main()
{
    explain_unordered_set();
    return 0;
}

void explain_unordered_set()
{
    // same as set but is not sorted
    //  lower_bound and upper_bound function does not work, rest all function are same
    // it does not stores in any particular order, it has a better complexity than set in most cases, except some when collisions happens
}